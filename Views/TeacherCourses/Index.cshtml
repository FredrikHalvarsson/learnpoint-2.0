@model IEnumerable<learnpoint_2._0.Models.TeacherCourses>

@{
    ViewData["Title"] = "Index";
}

<h1>Teacher Courses</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Teacher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.TeacherName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TeacherCoursesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TeacherCoursesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeacherCoursesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
